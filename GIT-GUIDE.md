# Git — მოკლე სახელმძღვანელო

Git არის ვერსიების კონტროლის სისტემა. ის ინახავს კოდის ისტორიას და საშუალებას გაძლევთ სხვებთან ერთად იმუშაოთ ერთ პროექტზე.

---

## ტერმინალის საბაზისო ბრძანებები

ჯერ ტერმინალში ნავიგაცია ვისწავლოთ:

| ბრძანება | რას აკეთებს | მაგალითი |
|----------|------------|----------|
| `pwd` | აჩვენებს სად ხარ | `pwd` |
| `ls` | აჩვენებს ფაილებს | `ls` |
| `cd საქაღალდე` | შედის საქაღალდეში | `cd Desktop` |
| `cd ..` | უკან, ერთი დონით ზემოთ | `cd ..` |
| `clear` | ასუფთავებს ტერმინალს | `clear` |

---

## Git-ის ყველაზე მნიშვნელოვანი ბრძანებები

### რეპოზიტორიის ჩამოტვირთვა (ერთხელ)

```bash
git clone <URL>
```

მაგალითი:
```bash
git clone https://github.com/khurtsilavabatu1/Skillwill-Javascript.git
```

ეს ქმნის საქაღალდეს თქვენს კომპიუტერზე პროექტის სრული ასლით.

---

### ბოლო ცვლილებების ჩამოტვირთვა

```bash
git pull
```

ამას ყოველ ლექციამდე აკეთებთ. უნდა იყოთ პროექტის საქაღალდეში.

---

### რა შეიცვალა?

```bash
git status
```

აჩვენებს რომელი ფაილები შეიცვალა, დაემატა ან წაიშალა.

---

### ისტორიის ნახვა

```bash
git log --oneline
```

აჩვენებს კომიტების (შენახვების) ისტორიას მოკლედ.

---

## როცა თქვენი ცვლილებების შენახვა გინდათ

### ნაბიჯი 1: ფაილების მომზადება

```bash
git add .
```

წერტილი (`.`) ნიშნავს "ყველა შეცვლილი ფაილი". კონკრეტული ფაილისთვის:

```bash
git add script.js
```

### ნაბიჯი 2: კომიტი (შენახვა ისტორიაში)

```bash
git commit -m "დავამატე ახალი ფუნქცია"
```

`-m` შემდეგ ბრჭყალებში წერთ რა შეცვალეთ.

---

## დამატებითი სასარგებლო ბრძანებები

| ბრძანება | რას აკეთებს |
|----------|------------|
| `git diff` | აჩვენებს ზუსტად რა შეიცვალა ფაილებში |
| `git stash` | დროებით ინახავს თქვენს ცვლილებებს გვერდზე |
| `git stash pop` | აბრუნებს დროებით შენახულ ცვლილებებს |
| `git branch` | აჩვენებს ბრენჩებს (განშტოებებს) |

---

## ტიპიური სამუშაო პროცესი

```
1. გახსენი ტერმინალი
2. cd Skillwill-Javascript
3. git pull                    ← ბოლო მასალის ჩამოტვირთვა
4. code .                      ← VS Code-ში გახსნა
5. ... მუშაობ ფაილებზე ...
6. git add .                   ← ცვლილებების მომზადება
7. git commit -m "შეტყობინება" ← შენახვა
```

---

## რჩევები

- **ყოველთვის** `git pull` ლექციამდე
- კომიტის შეტყობინება უნდა იყოს მოკლე და გასაგები
- თუ რამე გაფუჭდა — არ ინერვიულოთ, Git ყველაფერს ინახავს და შეცდომის გამოსწორება ყოველთვის შესაძლებელია
